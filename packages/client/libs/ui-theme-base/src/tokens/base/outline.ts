import { TokenMap } from '@noodlestan/ui-tokens';

export const outline: TokenMap = {
    '--outline-radius': 'var(--radius-m)',
    '--outline-color': 'var(--color-focus-1)',
    '--outline-base': '2px solid var(--outline-color)',
    '--outline-offset': 'var(--space-gap-0)',
};
